recipe: default.v1
assistant_id: concert_bot
language: en

pipeline:
  - name: "LLMDrivenNLU"

policies:
  - name: TEDPolicy
    max_history: 5
    epochs: 200
    batch_size: 50
    max_training_samples: 300
  - name: MemoizationPolicy
  - name: RulePolicy
